# include <stdio.h>
#include <math.h>
#include "chemistry.h.h"
#include "../data/data.h"
#include "../main/main.h"


double odleglosc()
{
    double dx = ;
    double dy = ;
    double dz = ;

    return sqrt(dx * dx + dy * dy + dz * dz);
}

